# 桌游项目旁观模式改造路线与实现依据

## 1. 文档目的

本文档用于指导 TableGame 项目为全部服务器权威桌游增加旁观功能，作为后续架构设计、分阶段开发、测试、部署和验收的统一依据。

### 当前实施进度（2026-09-01）

- 阶段1已经完成：共享成员角色、SQLite兼容恢复、旁观加入、独立广播、行动拦截、换席接口、房主设置、移出成员和玩家活动生命周期均已落地；
- 所有旁观能力继续受“引擎完整能力声明 + `SPECTATORS_ENABLED=1`”双重开关保护；
- 阶段2已经完成：`game9` 已接入第一套真实旁观视图、准备阶段换席逻辑、身份选择、玩家席/旁观席页面、房主开关和移动端折叠；
- 阶段3已经完成：`game9` 已通过服务器灰度和真实多设备联机验收；
- 阶段4已经完成：`shared/client/spectator-ui.js` 和 `shared/styles/spectator.css` 已承接加入身份、身份提示、旁观席、换席、房主设置、统一错误和移动端折叠，`game9` 已改用共享模块；
- 阶段5的首个游戏 `game6` 已完成代码迁移、服务器灰度和真实多设备联机验收；
- `game14` 已完成代码迁移、服务器灰度和真实多设备联机验收；
- `game15` 已完成代码迁移和自动化验收：独立旁观视图、牌架及事务草稿隔离、安全换席、通用旁观UI、并发换座与SQLite恢复均已落地，下一步为服务器灰度与真实多设备验收。
- `game3` 已完成代码迁移和自动化验收：公共骰子及计分视图、只读旁观权限、安全换席、通用旁观UI、并发换座与SQLite恢复均已落地，下一步为服务器灰度与真实多设备验收。

旁观功能的核心目标是：

- 玩家席已满后，新加入成员可以进入旁观席；
- 用户加入房间时可以主动选择旁观；
- 准备阶段的正式玩家可以主动离开玩家席并转入旁观席；
- 旁观者不计入开局人数，不参与回合、计分和胜负；
- 旁观者只能看到公共信息，不能看到任何玩家私密信息；
- 旁观者不能提交游戏规则行动；
- 旁观身份能够断线恢复并持久化；
- 旁观连接不能使已经没有正式玩家的废弃房间永久存活。

这不是简单放宽房间人数限制，而是一次共享房间身份系统升级。新的核心关系是：

```text
room.members
├── player      正式玩家
└── spectator   旁观者

state.players
└── 只包含真正参与游戏的正式玩家
```

---

## 2. 第一版统一规则口径

第一版旁观功能统一采用以下规则：

1. 每个房间最多 10 名旁观者。
2. 加入房间时可以主动选择“作为玩家加入”或“作为旁观者加入”。
3. 用户选择作为玩家加入，但玩家席已满时，自动转入旁观席，并给出明确提示。
4. 游戏开始后，新成员只能作为旁观者加入。
5. 只有游戏真正处于准备大厅时才能在玩家席和旁观席之间切换。
6. 正式玩家转入旁观席后，不再为开局人数达标作贡献。
7. 旁观者不会自动补入空座位，必须主动选择进入玩家席。
8. 游戏进行中的掉线玩家仍然保留原座位，旁观者不能顶替。
9. 房主不能转入旁观席。
10. 第一版暂不实现房主转让。
11. 房主可以关闭后续旁观者加入。
12. 关闭旁观不会自动移出已经在房间中的旁观者，房主可单独移出。
13. 房主可以移出正式玩家和旁观者。
14. 旁观者只能接收公共牌局信息。
15. 旁观者不能提交任何游戏规则行动。
16. 只有旁观者在线时，不能无限延长房间生命周期。

### 2.1 加入房间的判定

| 房间情况 | 用户意图 | 处理结果 |
|---|---|---|
| 准备阶段且有空位 | 作为玩家加入 | 进入玩家席 |
| 准备阶段且玩家已满 | 作为玩家加入 | 自动进入旁观席并提示 |
| 准备阶段 | 主动选择旁观 | 直接进入旁观席 |
| 游戏已经开始 | 任意新用户加入 | 只能进入旁观席 |
| 房主关闭旁观 | 加入旁观席 | 拒绝加入 |
| 旁观人数达到上限 | 加入旁观席 | 拒绝加入 |
| 游戏尚未支持旁观 | 玩家席已满或游戏已开始 | 保持旧版拒绝逻辑 |

加入页面应明确提供：

```text
○ 作为玩家加入
○ 作为旁观者加入
```

满房自动转为旁观者时必须明确显示：

```text
玩家席已满，你已进入旁观席。
```

### 2.2 玩家转入旁观席

只允许在真正的准备阶段执行：

- 玩家点击“转入旁观席”；
- 玩家从 `state.players` 中移除；
- 原座位成为空位；
- 该成员在 `room.members` 中的角色改为 `spectator`；
- 其他旁观者不会自动补位；
- 房主不能执行此操作。

以下阶段不允许转入旁观席：

- 游戏进行中；
- 单局结算阶段；
- 固定多局比赛的两局之间；
- 任何会改变既有回合、分数、牌库或先手顺序的阶段。

### 2.3 旁观者进入玩家席

只有同时满足以下条件时才允许：

- 游戏处于真正的准备大厅；
- 玩家席有空位；
- 旁观者主动点击“进入玩家席”；
- 两个并发请求争夺最后一个座位时，只允许一个成功。

---

## 3. 兼容现有游戏的总体策略

旁观功能不能一次强制应用到全部游戏。共享服务器应使用引擎能力声明，逐个游戏开放。

建议游戏引擎增加：

```js
export const SUPPORTS_SPECTATORS = true;
```

并实现：

```js
buildSpectatorView(state)
canChangeSeats(state)
vacateSeat(state, playerId)
```

共享服务器按照能力判断：

```text
游戏声明支持旁观
→ 启用旁观加入、旁观视图和座位切换

游戏没有声明支持旁观
→ 保持现有房间满员和游戏开始后的拒绝逻辑
```

第一阶段继续使用协议 v3，新增字段和接口采用向后兼容方式。等全部游戏迁移完成后，再决定是否需要统一升级协议版本。

同时增加按游戏控制的功能开关：

```ini
SPECTATORS_ENABLED=1
SPECTATOR_LIMIT=10
```

线上出现异常时可以快速切换为：

```ini
SPECTATORS_ENABLED=0
```

然后只重启对应游戏服务，不必回滚全部共享代码。

---

## 4. 旁观者视图与隐藏信息边界

旁观者必须获得独立的公共视图，不能借用任何现有玩家的视图。

正确方式：

```js
engine.buildView(state, playerId)
engine.buildSpectatorView(state)
```

错误方式：

```js
engine.buildView(state, hostId)
engine.buildView(state, somePlayerId)
```

也不能只在共享服务器中通用地删除 `hand` 字段，因为不同游戏的隐藏信息结构完全不同。

### 4.1 旁观者可以看到

- 玩家昵称；
- 在线状态；
- 房主身份；
- 当前行动玩家；
- 公共牌桌；
- 各玩家手牌数量；
- 公开弃牌；
- 公开分数和胜场；
- 公开行动结果；
- 公共倒计时；
- 经过审查的公开日志；
- 单局和整场比赛结果。

### 4.2 旁观者不能看到

- 任意玩家的具体手牌；
- 暗牌数值；
- 私密抽牌；
- 私密查看结果；
- 只发给目标玩家的操作提示；
- 尚未确认的操作草稿；
- 私密保留牌；
- 玩家恢复令牌；
- 任意其他会扩大公共信息范围的字段。

### 4.3 重点游戏的信息边界

- `game9`：按照现行规则隐藏其他玩家尚未公开的筹码等信息。
- `game8`：市场明牌保留可以公开，暗牌保留仍必须隐藏。
- `game11`：必须逐字段审查，不能只生成房主视图和普通玩家视图。
- `game13`：隐藏所有背面牌值、初始查看、PEEK、SPY、SWAP、待处理抽牌和私密目标提示。
- `game14`：只能看到玩家手牌数量，不能看到具体行动牌。
- `game15`：只能看到确认后的公共牌桌，不能看到牌架和当前玩家未确认的草稿。

所有游戏日志也必须单独检查，避免日志文本泄露本应私密的牌值或目标。

---

## 5. 第一阶段：改造共享房间系统

第一阶段先建立通用基础设施，默认不为任何未迁移游戏开放旁观。

### 5.1 成员身份模型

将共享成员结构扩展为：

```js
{
  name,
  role: "player" | "spectator",
  resumeToken,
  connected,
  everConnected,
  kicked,
  actionIds
}
```

正式游戏状态继续只保存正式玩家。

### 5.2 SQLite向后兼容

旧房间没有 `role` 字段，恢复时自动推断：

```text
成员ID存在于state.players中
→ role = player

成员ID不存在于state.players中
→ role = spectator
```

不应仅为增加可选角色字段就废弃全部旧房间。

### 5.3 修改加入流程

共享服务器需要支持：

- 恢复旧玩家身份；
- 恢复旧旁观者身份；
- 主动旁观加入；
- 满房自动转旁观；
- 游戏开始后转旁观；
- 旁观功能关闭时拒绝；
- 旁观人数达到上限时拒绝；
- 未迁移游戏保持原行为。

### 5.4 修改广播范围

广播对象从：

```js
room.state.players
```

改为全部有效房间成员：

```text
player
→ engine.buildView(state, playerId)

spectator
→ engine.buildSpectatorView(state)
```

共享层统一向视图附加：

```js
{
  roomRole: "player" | "spectator",
  spectators: [
    { id, name, connected }
  ],
  spectatorCount,
  spectatorLimit,
  allowSpectators
}
```

### 5.5 新增房间级接口

建议增加：

```text
POST /api/seat
```

转入旁观席：

```json
{
  "intent": "spectate"
}
```

进入玩家席：

```json
{
  "intent": "play"
}
```

增加房主设置接口：

```text
POST /api/room-settings
```

例如：

```json
{
  "allowSpectators": false
}
```

座位和旁观设置属于共享房间系统，不应作为各游戏的规则行动进入 `applyAction()`。

### 5.6 阻止旁观者提交行动

旁观者访问：

```text
POST /api/actions
```

必须由共享服务器直接返回：

```text
403 spectator_cannot_act
```

不能只依赖浏览器隐藏按钮。

### 5.7 移出成员

房主移出正式玩家时：

- 调用游戏引擎移除玩家；
- 更新游戏状态；
- 更新成员状态；
- 持久化并广播。

房主移出旁观者时：

- 不调用游戏引擎的玩家移除逻辑；
- 标记成员被移出；
- 发送被移出消息；
- 关闭其SSE连接；
- 持久化并广播。

---

## 6. 房间生命周期调整

旁观者不能单独使房间永久存活。

建议在房间快照中增加：

```js
lastPlayerActivityAt
```

以下行为更新玩家活动时间：

- 正式玩家加入玩家席；
- 正式玩家上线或下线；
- 正式玩家提交游戏行动；
- 服务端执行游戏超时；
- 游戏正式状态发生变化。

以下行为不更新玩家活动时间：

- 旁观者加入；
- 旁观者上线或下线；
- 旁观者刷新页面；
- 旁观者恢复身份。

清理条件建议调整为：

```text
没有正式玩家在线
并且
当前时间 - lastPlayerActivityAt >= roomIdleMs
→ 删除房间
```

即使仍有旁观者保持连接，房间到期后也应：

1. 向旁观者发送房间已过期消息；
2. 关闭旁观者SSE连接；
3. 清除房间倒计时；
4. 从内存删除房间；
5. 从SQLite删除房间。

后续还可以进一步实现“最后一名正式玩家离线后暂停游戏倒计时”，但这不作为第一版旁观功能的强制前置任务。

---

## 7. 资源与安全限制

旁观者不会复制完整游戏状态，但每名在线旁观者仍会占用：

- 一条SSE长连接；
- 一个心跳计时器；
- 一份成员身份；
- 广播序列化和网络流量。

第一版限制：

```text
每个房间最多10名旁观者
```

后续可增加：

- 同一IP短时间加入频率限制；
- 房间总成员上限；
- 无效房间号请求限制；
- 无效恢复令牌请求限制；
- 旁观密码；
- 房主批准旁观；
- 更长或更难猜测的房间号。

第一版至少应具备：

- 房主可关闭旁观；
- 房主可移出旁观者；
- 每房旁观人数上限；
- 旁观者行动在服务器端被拒绝。

---

## 8. 第二阶段：以game9建立完整样板

`game9` 状态和回合结构相对简单，作为第一个旁观改造样板。

### 8.1 game9引擎改造

增加：

```js
SUPPORTS_SPECTATORS
buildSpectatorView()
canChangeSeats()
vacateSeat()
```

旁观视图仅包含game9规则允许公开的内容：

- 当前数字牌；
- 公共筹码池；
- 牌库数量；
- 玩家已经获得的数字牌；
- 按现行规则公开的筹码信息；
- 当前行动者；
- 倒计时；
- 公共日志；
- 结算结果。

必须验证其他玩家当前不公开的信息不会发送给旁观者。

### 8.2 game9页面改造

加入界面增加玩家/旁观选择。

房间页面区分：

```text
玩家席 3/4
- 房主
- 玩家A
- 玩家B

旁观席 2/10
- 旁观者甲
- 旁观者乙
```

旁观者页面：

- 显示“你正在旁观”；
- 不显示游戏行动按钮；
- 不显示玩家私密信息；
- 玩家席有空位时显示“进入玩家席”；
- 保留公共牌桌、倒计时和公开记录；
- 移动端旁观者列表默认可折叠。

### 8.3 game9自动化测试矩阵

至少覆盖：

1. 有空位时正常作为玩家加入；
2. 满房后自动成为旁观者；
3. 游戏开始后新成员只能旁观；
4. 准备阶段玩家主动转入旁观席；
5. 旁观者主动进入空座位；
6. 房主不能转入旁观席；
7. 旁观者伪造游戏行动返回403；
8. 旁观者看不到隐藏信息；
9. 房主可以移出旁观者；
10. 关闭旁观后新旁观者不能进入；
11. 旁观人数达到上限后拒绝加入；
12. SQLite重启恢复旁观身份；
13. 原浏览器刷新后恢复旁观身份；
14. 两名旁观者同时争夺最后一个座位时只有一人成功；
15. 只有旁观者在线时房间仍会按规则过期；
16. 原有game9规则和持久化测试全部继续通过。

---

## 9. 第三阶段：game9真实联机测试

共享框架首次部署时，只为game9开启：

```ini
SPECTATORS_ENABLED=1
SPECTATOR_LIMIT=10
```

其他游戏继续关闭：

```ini
SPECTATORS_ENABLED=0
```

真实测试重点：

- 2～3名正式玩家；
- 多个电脑和手机旁观者；
- 满房自动旁观；
- 主动旁观加入；
- 玩家和旁观者反复换座；
- 旁观者刷新与断线恢复；
- 房主关闭旁观；
- 房主移出旁观者；
- game9服务进程重启；
- SQLite恢复角色；
- 只有旁观者在线时房间过期；
- 长时间SSE连接稳定性；
- 任意隐藏信息泄露检查。

至少完成一轮真实联机稳定测试后，再迁移其他游戏。

---

## 10. 第四阶段：提取通用前端能力

game9样板稳定后，将重复逻辑提取到共享客户端，例如：

```text
shared/client/spectator-ui.js
shared/client/seat-controller.js
```

通用模块负责：

- 加入身份选择；
- 旁观身份徽章；
- 玩家席和旁观席渲染；
- 进入玩家席；
- 转入旁观席；
- 房主旁观设置；
- 旁观者被移出；
- 房间过期提示；
- 统一错误提示。

各游戏只负责页面布局位置和视觉风格，不重复实现房间身份协议。

---

## 11. 第五阶段：分批迁移其他游戏

目前接入统一服务器权威框架、可以迁移的项目包括：

```text
game
game2
game3
game5
game6
game8
game9
game10
game11
game13
game14
game15
```

### 11.1 第一批：信息结构相对直接

建议优先：

```text
game6
game14
game15
```

重点：

- `game6`（迁移及真实联机验收完成）：旁观者只看公共牌列、全员锁牌后的公开出牌和玩家手牌数量；
- `game14`（迁移及真实联机验收完成）：旁观者不能看到任何玩家具体手牌，只在服务器确认换牌后看到被公开的三张旧牌；
- `game15`（代码迁移完成，待真实联机验收）：旁观者不能看到牌架、摸牌结果和未确认草稿，只看确认后的公共牌桌。

### 11.2 第二批：其他常规权威游戏

```text
game
game2
game5
game10
```

`game3` 已提前完成代码迁移，待真实联机验收后从本批移出。

迁移前逐个审查：

- `buildView()` 中的私密字段；
- 日志内容；
- 当前行动提示；
- 是否以当前玩家为中心旋转座位；
- 是否存在局间保留的私密状态。

### 11.3 第三批：隐藏信息复杂项目

最后处理：

```text
game8
game11
game13
```

必须重点审查：

- 暗牌；
- 私密抽牌；
- 私密保留区；
- 查看类能力；
- 交换类能力；
- 只发给目标玩家的提示；
- 玩家专属操作窗口；
- 私密日志。

### 11.4 暂不处理

```text
game4
game7
game12
```

这些项目尚未完成统一服务器权威改造，应在权威化完成后再接入旁观系统。

---

## 12. 每个游戏的固定迁移清单

每迁移一个游戏，都按照以下步骤执行：

1. 声明支持旁观；
2. 实现独立公共旁观视图；
3. 审查所有私密字段；
4. 审查日志是否泄露信息；
5. 实现准备阶段座位退出；
6. 确认房主不能离开玩家席；
7. 确认旁观者不进入回合顺序；
8. 确认旁观者不影响开局人数；
9. 确认旁观者不参与计分和胜负；
10. 接入通用旁观UI；
11. 增加隐藏信息隔离测试；
12. 增加旁观者伪造行动测试；
13. 增加SQLite角色恢复测试；
14. 增加换座并发测试；
15. 运行该游戏原有全部测试；
16. 运行共享服务器回归测试；
17. 本地多浏览器测试；
18. 推送GitHub；
19. 服务器备份该游戏数据库；
20. 服务器拉取代码并测试；
21. 只重启该游戏服务；
22. 开启该游戏的旁观环境变量；
23. 进行真实玩家验收；
24. 稳定后再迁移下一个游戏。

---

## 13. 服务器发布与回滚策略

### 13.1 首次发布共享框架

1. 等待正在进行的目标游戏结束；
2. 备份game9数据库；
3. 拉取共享框架和game9代码；
4. 运行共享服务器测试；
5. 运行game9全部测试；
6. 只在game9环境文件开启旁观；
7. 只重启game9；
8. 检查健康接口、日志和端口；
9. 完成真实旁观测试；
10. 稳定后逐个开放其他游戏。

数据库备份示例：

```bash
sudo systemctl stop tablegame@game9.service
sudo cp -a /var/lib/tablegame/game9 /var/backups/game9-spectator-test
sudo systemctl start tablegame@game9.service
```

### 13.2 功能关闭

出现旁观相关异常时，优先关闭功能而不是立刻降级共享代码：

```ini
SPECTATORS_ENABLED=0
```

然后：

```bash
sudo systemctl restart tablegame@game9.service
```

这样可以保留代码和持久化兼容能力，同时阻止新的旁观加入和换座。

### 13.3 回滚注意事项

共享服务器旧版本不知道如何为旁观者生成视图。已经产生旁观成员的SQLite房间不能在未经处理的情况下直接交给旧版服务器运行。

因此回滚优先级应为：

1. 使用环境变量关闭功能；
2. 保持支持角色字段的新共享服务器；
3. 必须降级代码时，先清理或迁移包含旁观成员的测试房间；
4. 再恢复旧代码和数据库备份。

---

## 14. 完整验收标准

整个旁观改造满足以下条件后才算完成：

- 满房加入能够稳定自动进入旁观席；
- 用户可以主动以旁观者身份加入；
- 准备阶段可以安全地在玩家席和旁观席之间切换；
- 房主始终保留在玩家席；
- 两人争夺最后一个座位时不会产生超额玩家；
- 玩家和旁观身份能够断线恢复；
- Node进程和服务器重启后身份能够从SQLite恢复；
- 旁观者无法提交任何游戏规则行动；
- 房主可以关闭旁观并移出旁观者；
- 每个游戏都有独立的旁观视图；
- 不使用任意玩家视图冒充旁观视图；
- 私密手牌、暗牌、查看结果、保留牌、目标提示和草稿全部隔离；
- 日志不泄露私密信息；
- 旁观者不参与开局人数、回合、计分和胜负；
- 旁观者不能顶替进行中掉线玩家；
- 旁观者不能单独使废弃房间永久存活；
- 每房旁观人数受到限制；
- 桌面端和移动端都能清楚识别当前身份；
- 旁观功能可以通过环境变量快速关闭；
- 原有玩家联机流程和全部规则测试没有回归。

---

## 15. 推荐执行顺序总览

```text
阶段0：锁定规则和接口
    ↓
阶段1：共享成员角色、加入、广播、座位接口、生命周期和持久化
    ↓
阶段2：game9完整旁观样板
    ↓
阶段3：game9本地测试、服务器灰度和真实联机测试
    ↓
阶段4：提取共享旁观客户端组件
    ↓
阶段5：game6、game14、game15
    ↓
阶段6：game、game2、game3、game5、game10
    ↓
阶段7：game8、game11、game13
    ↓
阶段8：统一UI、全项目回归和最终验收
```

下一次开始具体开发时，应从阶段1开始：先改造共享服务器、共享客户端和持久化结构，默认关闭旁观功能；随后立即用game9完成第一个可上线的旁观样板。
